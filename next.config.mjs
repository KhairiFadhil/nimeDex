/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns:[
            {hostname: "mangadex.org"},
            
            
        ]  
    }
};

export default nextConfig;
